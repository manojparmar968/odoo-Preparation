# pwd --> To Know the path

# Sudo apt-get install git
# git config --global user.name "Manoj Parmar"
# git config --global user.email ""

# To Generate ssh-rsa key
    ssh-keygen -t rsa -b 4096 -C "email ID"
        > password
        > confirm password
    eval "$(ssh-agent -s)"
    ssh-add `/.ssh/id_rsa
        > Enter Password
    cat `/.ssh/id_rsa.pub
        > ssh-rsa key add into your git account to get access.

# git clone -b branchName url OR git clone --recursive (Take clone with Sub folder)
# git branch
# git add .
# git commit -m "meaning full commit message"
# git push OR git push branch name OR git push -u branch name
# git pull
# git stash
# git stash pop
# git checkout branchName

# create new branch
    git branch branchName
    git checkout -b BranchName --> Branch will create & checkout on that branch.

# To store git global id & password
    git config --global credential.helper store

# To remove git global credential
    git config --global --unset credential.helper

# Force Fully code push
    git push -f branch name

# To add git Https Link
    git remote add origin url

# To remove git link
    git remote remove

# To know the name by the commit id
    git show --pretty

# Git Revert OR Undo the commit
    git log
    git revert commit_id

# Git Commands
    When Remove folder from git not from local
        git rm --cached -r folderName
    When remove folder from both git & local
        git rm -r folderName/FileName
        git commit -m "module is removed not in use"
        git push origin branch name
    sudo rm -rf